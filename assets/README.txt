Put your Nofar logo here as  nofar_logo.png  (PNG, transparent background ideal).
It appears on the login screen, the sidebar and the hero band.
If the file is absent the app shows a clean "NofarUSA" wordmark fallback, so the
app still runs without it.
